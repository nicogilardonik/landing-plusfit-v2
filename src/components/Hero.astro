---
import { Play, ArrowRight } from "lucide-astro";
---

<header
    id="top"
    class="relative h-screen min-h-[700px] flex items-center justify-center overflow-hidden"
>
    <!-- Cinematic Background -->
    <div class="absolute inset-0 z-0">
        <img
            src="https://images.unsplash.com/photo-1517836357463-d25dfeac3438?q=80&w=2070&auto=format&fit=crop"
            alt="Cinematic Gym"
            class="w-full h-full object-cover scale-105 animate-slow-zoom filter brightness-50 contrast-125"
        />
        <!-- Advanced Gradient Overlay -->
        <div
            class="absolute inset-0 bg-gradient-to-t from-[#0a0a0a] via-black/40 to-black/30"
        >
        </div>
        <div
            class="absolute inset-0 bg-gradient-to-r from-black/80 via-transparent to-black/80"
        >
        </div>
    </div>

    <div class="relative z-10 container mx-auto px-6 text-center pt-20">
        <div
            class="inline-flex items-center gap-2 px-4 py-2 rounded-full border border-white/20 bg-white/5 backdrop-blur-md text-yellow-400 font-bold text-xs uppercase tracking-widest mb-8 animate-fade-in-up"
        >
            <span class="w-2 h-2 rounded-full bg-yellow-500 animate-pulse"
            ></span>
            Construye la mejor versión de ti mismo
        </div>

        <h1
            class="text-2xl md:text-7xl font-black text-white leading-none mb-6 tracking-wide mix-blend-overlay opacity-90 animate-fade-in-up delay-100"
        >
            Tu familia fitness te espera
        </h1>

        <p
            class="text-xl text-gray-300 max-w-xl mx-auto mb-12 font-light leading-relaxed animate-fade-in-up delay-300"
        >
            Entrena con atención personalizada en un ambiente divertido y
            familiar
        </p>
    </div>

    <!-- Scroll Indicator -->
    <div
        class="absolute bottom-10 left-1/2 -translate-x-1/2 flex flex-col items-center gap-2 opacity-50 animate-bounce"
    >
        <span class="text-[10px] text-white uppercase tracking-[0.2em]"
            >Scroll</span
        >
        <div class="w-px h-12 bg-gradient-to-b from-white to-transparent"></div>
    </div>
</header>
