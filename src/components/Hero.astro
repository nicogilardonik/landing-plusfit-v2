---
import { Play, ArrowRight } from "lucide-astro";
---

<header
    id="top"
    class="relative h-screen min-h-[800px] flex items-center justify-center overflow-hidden"
>
    <!-- Cinematic Background -->
    <div class="absolute inset-0 z-0">
        <img
            src="https://images.unsplash.com/photo-1517836357463-d25dfeac3438?q=80&w=2070&auto=format&fit=crop"
            alt="Cinematic Gym"
            class="w-full h-full object-cover scale-105 animate-slow-zoom"
            style="filter: brightness(0.3) contrast(1.2) grayscale(0.2);"
        />
        <!-- Advanced Gradient Overlay -->
        <div class="absolute inset-0 bg-premium-gradient opacity-90"></div>
    </div>

    <div class="relative z-10 container mx-auto px-6 text-center pt-20">
        <!-- Floating Badge -->
        <div
            class="inline-flex items-center gap-3 px-6 py-2 rounded-full border border-white/10 bg-white/5 backdrop-blur-md text-brand-yellow font-bold text-xs uppercase tracking-[0.2em] mb-12 animate-fade-in-up"
        >
            <span class="w-2 h-2 rounded-full bg-brand-yellow animate-pulse-glow shadow-[0_0_10px_#FACC15]"></span>
            Nueva Era Fitness
        </div>

        <!-- Main Headline -->
        <h1
            class="text-5xl md:text-8xl lg:text-9xl font-heading font-black text-white leading-none mb-8 tracking-wide animate-fade-in-up delay-100 drop-shadow-2xl"
        >
            TRAIN LIKE <br/>
            <span class="text-transparent bg-clip-text bg-gradient-to-r from-brand-yellow via-white to-brand-yellow">A PRO</span>
        </h1>

        <!-- Subtext -->
        <p
            class="text-xl md:text-2xl text-gray-300 max-w-2xl mx-auto mb-12 font-light leading-relaxed animate-fade-in-up delay-300 mix-blend-plus-lighter"
        >
            Atención personalizada en un ambiente diseñado para tu mejor versión.
        </p>

        <!-- CTA Buttons -->
        <div class="flex flex-col md:flex-row gap-6 justify-center items-center animate-fade-in-up delay-500">
            <a href="#planes" class="group relative px-8 py-4 !bg-brand-yellow !text-black font-black uppercase tracking-widest rounded-full overflow-hidden transition-all hover:scale-105 hover:shadow-[0_0_40px_rgba(250,204,21,0.6)] ring-2 ring-transparent hover:ring-white/50">
                <span class="relative z-10 flex items-center gap-2">
                    Empezar Ahora <ArrowRight class="w-5 h-5 group-hover:translate-x-1 transition-transform stroke-2" />
                </span>
                <div class="absolute inset-0 bg-white/50 translate-y-full group-hover:translate-y-0 transition-transform duration-300"></div>
            </a>
            
            <button class="px-8 py-4 rounded-full border-2 border-white/20 text-white font-bold uppercase tracking-widest hover:bg-white/10 transition-all backdrop-blur-sm flex items-center gap-2 group hover:border-brand-yellow/50">
                <Play class="w-5 h-5 fill-white group-hover:scale-110 transition-transform" /> 
                Ver Video
            </button>
        </div>
    </div>

    <!-- Scroll Indicator -->
    <div
        class="absolute bottom-10 left-1/2 -translate-x-1/2 flex flex-col items-center gap-4 opacity-50 animate-float"
    >
        <div class="w-[1px] h-24 bg-gradient-to-b from-transparent via-brand-yellow to-transparent opacity-50"></div>
        <span class="text-[10px] text-brand-yellow uppercase tracking-[0.3em] rotate-90 origin-left translate-y-8"
            >Scroll</span
        >
    </div>
</header>
